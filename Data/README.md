 Most datasets are provided in `.csv` format. Due to size constraints, two datasets (BPIC17 and BPIC19) are provided as compressed `.zip` files.

  **Notes**
  - The pipeline will automatically extract these `.zip` files when needed.
  - If you prefer to do it manually, unzip the files in this folder before running the code.
